#WAP combine all the first characters from given list representing a some of the
# compelling qualities of a great leader.
#qualities = [“Passion”, “Yearning”, “Transparency”, “Humble”, “open-minded”, “Noble”]
def qual(qualities):
    quality = []
    for y in range(6):
        y= input(">")
        quality.append(y)
        lead = []
    for x in quality:
        lead.append(x[0])
    return lead
print("grate leader is",qual([]))